/*
 * 控制流
 * while语句
 * 条件为真时，函数体就会被执行 
 * 条件为假时，跳出循环
 * 一般为不定循环次数
 */ 
#include <iostream>

int main()
{
    int sum = 0, val = 1;
    // 只要val的值小于等于10，while循环就持续执行
        while (val <= 10) {
        sum += val;  // sum = sum + val  
        ++val;       // 先++，再取值
    }
    std::cout << "Sum of 1 to 10 inclusive is " 
              << sum << std::endl;

	return 0;
}

